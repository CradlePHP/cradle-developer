<?php //-->
/**
 * This file is part of a Custom Package.
 */

/**
 * Creates a [[name]]
 *
 * @param Request $request
 * @param Response $response
 */
$this->on('[[name]]-create', function ($request, $response) {});

/**
 * Creates a [[name]]
 *
 * @param Request $request
 * @param Response $response
 */
$this->on('[[name]]-detail', function ($request, $response) {});

/**
 * Removes a [[name]]
 *
 * @param Request $request
 * @param Response $response
 */
$this->on('[[name]]-remove', function ($request, $response) {});

/**
 * Restores a [[name]]
 *
 * @param Request $request
 * @param Response $response
 */
$this->on('[[name]]-restore', function ($request, $response) {});

/**
 * Searches [[name]]
 *
 * @param Request $request
 * @param Response $response
 */
$this->on('[[name]]-search', function ($request, $response) {});

/**
 * Updates a [[name]]
 *
 * @param Request $request
 * @param Response $response
 */
$this->on('[[name]]-update', function ($request, $response) {});
